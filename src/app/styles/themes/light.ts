import { defaultTheme } from './default';

export const lightTheme = {
    ...defaultTheme,
    palette: {
        primary: {
            main: '#ffffff',
            contrast: '#000000'
        }
    }
};